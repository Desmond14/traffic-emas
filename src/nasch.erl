-module(nasch).

%% API
-export([]).

%%returns Car With Updated Position and UpdatedIntersection
%%follow_nasch(Car, Intersection) ->
%%%%  Calculate Dist to car ahead
%%  HasToDecelerate = has_to_decelerate(Car, DistToCarAhead)
%%  CanAccelerate = can_accelerate(Car, Intersection),
%%  if
%%    CanAccelerate ->
%%      accelerate
%%  end